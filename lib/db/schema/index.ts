// Table definitions live in this directory, one file per domain area, and are
// re-exported here so `drizzle-kit` and the `db` client see the whole schema.
//
// export * from "./organizations"
export {}
